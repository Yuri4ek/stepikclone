# КодСтарт — фронтенд

Платформа дистанционной подготовки школьников 1–9 классов по спортивному программированию (по типу Stepik).
React 19 + TypeScript + Vite + Tailwind CSS 4. Работает с бэкендом по его API (Swagger — `/docs`, описание срезов — [`backend/README.md`](../backend/README.md)).

## Запуск

```bash
cd frontend
npm install
echo 'VITE_API_URL=http://localhost:8000' > .env   # адрес сервера API (файл не коммитится)
npm run dev                  # http://localhost:5173
```

`VITE_API_URL` — адрес сервера API без `/api/v1` (по умолчанию `http://localhost:8000`). После изменения перезапустите `npm run dev`.
Сборка: `npm run build` → статика в `dist/` (для SPA нужен fallback всех путей на `index.html`).

Демо-входы (кнопки на странице входа, пароль `demo1234`): `student@`, `curator@`, `admin@example.com`.

## Что сделано

**Ученик**: главная, где крупнее всего — тёмная карточка «Следующий шаг»; рядом прогресс курсов с местом в группе,
серия дней и возвращённые работы с комментарием куратора; карта курса «где я и что дальше» (текущий шаг — синий ореол); оглавление со статусами; плеер шага с картой урока;
результат автопроверки сразу, у задач с тестами — вердикт по каждому тесту; статус ручной проверки и причина возврата;
«Вопрос куратору» на каждом шаге; «Прогресс и баллы» — итог, сегменты по источникам
(автопроверка / куратор / ещё можно получить), формула рейтинга и подсказка, какое задание даст баллы дальше.

**Куратор**: обзор (очередь, работы старше суток, «Выпадает» / «Замедлился»), очередь проверки — таблица с уровнем отставания
у имени ученика и временем ожидания; карточка работы (ответ в виде, зависящем от типа шага, критерии, быстрые баллы,
«Зачесть» / «Вернуть с комментарием», переход к следующей работе); «Вопросы» учеников по шагам с ответом;
«Ученики» — все ученики курсов куратора, три уровня (В графике, Замедлился, Выпадает) и причины.

**Администратор**: список курсов (вкл. черновики), создание курса, конструктор: модули → уроки → шаги, выбор типа шага,
редактор под каждый тип, предпросмотр «глазами ученика», порядок шагов, удаление, публикация, обложка курса,
загрузка картинок в теорию, паспорт курса, снятие с публикации, кураторы и ученики курса (назначить, записать, снять);
«Пользователи» — курсы каждого, запись и назначение на курс, создание пользователя.

**Общие страницы**: лендинг для гостей, «Как это работает» (типы шагов, проверка, статусы, FAQ), профиль, «Мои работы»
(все отправки с сервера со статусами, результатами тестов и комментариями, засчитанные задания, свои вопросы), страница 404.

**Дизайн — по брендбуку кейса (v1.1)**: токены из раздела 08 без изменений (`src/app/styles/tokens.css`, `tailwind.config.js`),
шрифты Onest и JetBrains Mono; шесть статусов «цвет + иконка» (`shared/ui/statusMeta.ts`); типы шагов — линейная иконка
в синем квадрате без своего цвета; плотность по ролям: у ученика кегль 18 и кнопки 56 (`.role-student`), у куратора
и администратора кегль 14–15 (`.role-staff`); градиент — только тёмное свечение на экране входа и в карточке «Следующий шаг»;
без эмодзи; красный — только «Не прошло тесты»; ученику пишем на «ты», куратору и администратору — на «вы».

## Архитектура — Feature-Sliced Design

Код в `src/` разложен по слоям [FSD](https://feature-sliced.design). Слой может импортировать только из слоёв ниже,
слайсы одного слоя друг друга не импортируют, импорт из чужого слайса — только через его `index.ts`. Алиас `@/` → `src/`.

```
app/        точка сборки: провайдеры, роутер и гарды доступа, глобальные стили
pages/      страницы (по слайсу на экран): landing, auth, catalog, course, step, course-builder, review, lag, admin-users, …
widgets/    составные блоки: app-layout, course-card, course-outline (карта курса + оглавление), next-step, rating-breakdown
features/   действия пользователя: auth, enroll-course, step-answer, review-submission, filter-by-course,
            create-course, manage-course, edit-course-structure, edit-step
entities/   бизнес-сущности: session, user, course, step (реестр типов шагов), submission, lag
shared/     без бизнес-логики: api (HTTP-клиент и типы контракта), ui (кит), lib (форматирование, useAsync, code-runner), config
```

Внутри слайса — сегменты `ui/`, `model/`, `api/`, `lib/`. Запросы к API лежат в сегменте `api` того слайса, которому принадлежат
(например, чтение курса — `entities/course`, запись на курс — `features/enroll-course`).

### Расширяемые типы шагов

Шаг на бэкенде = `kind` (механизм проверки, реестр `backend/app/steps/registry.py`) + произвольный JSON в `content`.
Тип шага для ученика = `content.type`; API отдаёт его полем `type` в оглавлении, шаге, очереди и расшифровке баллов.
Каждый тип — отдельный модуль в `src/entities/step/ui/step-types/` (редактор для администратора, плеер для ученика,
вид для куратора), список — в `src/entities/step/model/registry.ts`, выбор типа сгруппирован по разделам кейса.

| Тип | kind | Проверка |
|---|---|---|
| Теория | theory | при прочтении |
| Контрольный вопрос (один / несколько верных) | quiz | сразу |
| Вопрос с ответом числом | quiz | сразу |
| Scratch: разбор с ответом | quiz | сразу |
| Задача с тестами (Python) | code | прогон на сервере по всем тестам, сразу; «Проверить на примерах» — в браузере |
| Scratch: проект по ссылке | task | куратор |
| Minecraft Education (мир, скриншот, ссылка на MakeCode) | task | куратор |
| Проект (ссылка / скриншот / текст — по настройке) | task | куратор |

Новый тип на существующей проверке — один модуль и строка в реестре, без изменения API и базы. Шаг типа,
которого фронтенд не знает, открывается общим плеером своего `kind`.

Блочные программы Scratch / MakeCode пишутся в Markdown как ` ```blocks ` (одна строка — блок, отступ — вложенность,
`[Агент]` — раздел палитры) и рисуются компонентом `shared/ui/BlockProgram`.

## Ограничения / не успели

- Проверка решений на примерах в браузере (Pyodide из CDN) — только для удобства; засчитывает шаг сервер.
- Места в группе показываются по рейтингу курса; отдельной таблицы лидеров на фронтенде нет (эндпоинт `/progress/courses/{id}/leaderboard` есть).
- Числа из API (`Decimal`) приходят строками (`"10.00"`); HTTP-клиент (`shared/api/client.ts`) приводит их к `number`.
